This project loads Bootstrap 5 from a CDN by default (see index.html).
Drop a vendored copy of bootstrap.min.css / bootstrap.bundle.min.js here
and update the <link>/<script> paths in index.html if you'd rather serve
Bootstrap locally (e.g. for fully offline use).
